package LinkedList.Double;
public class insertAtRandom {
    //{ Driver Code Starts


class DLinkedList {

//     DNode newNode(DNode head, int data) {
//         DNode n = new DNode(data);
//         if (head == null) {
//             head = n;
//             return head;
//         }
//         head.next = n;
//         n.prev = head;
//         head = n;
//         return head;
//     }

//     void printList(DNode node) {
//         DNode temp = node;
//         while (temp.next != null) {
//             temp = temp.next;
//         }

//         while (temp.prev != null) {
//             temp = temp.prev;
//         }

//         while (temp != null) {
//             System.out.print(temp.data + " ");
//             temp = temp.next;
//         }
//         System.out.println();
//     }
//     DNode addNode(DNode head, int p, int x) {
//         // Directly get the node at position p.
//         DNode temp = getNodeAtPosition(head, p);

//         // Create the new node.
//         DNode n = new DNode(x);

//         // Adjust pointers for insertion.
//         DNode tempNext = temp.next;
//         n.next = tempNext;
//         n.prev = temp;

//         if (tempNext != null) {
//             tempNext.prev = n;
//         }
//         temp.next = n;

//         return head;
//     }

//     // Helper method to get the node at position p.
//     private DNode getNodeAtPosition(DNode head, int p) {
//         DNode temp = head;
//         while (p-- > 0 && temp != null) {
//             temp = temp.next;
//         }
//         return temp;
//     }
// }
}
}
    

// } Driver Code Ends


/* Structure of Doubly Linked List
class Node
{
    int data;
    Node next;
    Node prev;
    Node(int data)
    {
        this.data = data;
        next = prev = null;
    }
}*/


    

